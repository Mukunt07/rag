import { LoginForm } from "@/features/auth/components/login-form";

export const metadata = {
  title: "Login - KnowledgeHub AI",
  description: "Login to your KnowledgeHub AI workspace.",
};

export default function LoginPage() {
  return <LoginForm />;
}
