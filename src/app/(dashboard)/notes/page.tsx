import { NotesView } from "@/features/notes/components/notes-view";

export const metadata = {
  title: "Notes - KnowledgeHub AI",
  description: "View and manage your notes and summaries.",
};

export default function NotesPage() {
  return <NotesView />;
}
